# Kaki Logger

A personal web app for tracking daily events on a calendar.

## Getting Started

First, install dependencies:

```bash
npm install
```

Then, run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Features

- Calendar UI
- Log daily events with type and duration